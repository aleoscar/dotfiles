# colors
include ~/.config/newsboat/dark

datetime-format "%b %d %Y"

# max articles to cache
max-items 1000

browser firefox

# key bindings
bind-key j down
bind-key k up
bind-key u pageup
bind-key d pagedown
bind-key U show-urls
