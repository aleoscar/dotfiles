/* vim: set filetype=jsonc : */

{
    "layer": "top",
    "position": "bottom",
    "height": 30,
    "spacing": 12,
    "margin-top": 0,
    "modules-left": ["group/power", "custom/updates", "hyprland/workspaces", "hyprland/window", "custom/music"],
    "modules-right": ["network", "bluetooth", "clock"],

    "hyprland/workspaces": {
        "format": "{name}",
        "active-only": true,
        "show-special": true,
        "special-visible-only": true,
    },

    //jag vill ha conditional grejs här som det finns i workspaces
    //om class är tomt vill jag använda title t.ex
    //för spotify, men kanske för annat
    "hyprland/window": {
        "format": "{initialTitle}",
        "rewrite": {
            "(.*)Mozilla Firefox": "Firefox",
            "Spotify(.*)": "Spotify",
            "kitty": "Terminal",
            "(.*)Xournal(.*)": "Xournal++",
            "(.*)Thunar(.*)": "Thunar",
        },
        "max-length": 30,
        "icon": true,
        "icon-size": 16,
        "hide-empty-text": true,
    },

    // "wireplumber": {
    //     "format": "",
    //     "format-muted": " ",
    //     "hide-empty-text": true,
    // },

    "network": {
        "format": "󰖩",
        "format-disconnected": "󰖪",
        "tooltip-format": "{essid}",
        "on-click": "kitty nmtui"
    },

    "clock": {
        "format": "{:L%a %e %b  %H:%M}",
        "tooltip": false,
        "locale": "sv_SE.UTF8",
        "on-click": "kitty calcurse"
    },

    "bluetooth": {
        "format": "",
        "format-disabled": "󰂲", 
        "format-connected": " {device_alias}",
        "format-connected-battery": " {device_alias} {device_battery_percentage}%",
        "on-click": "blueman-manager",
    },

    "custom/updates": {
        "exec": "checkupdates | wc -l",
        "exec-if": "[[ $(checkupdates | wc -l) != 0 ]]",
        "format": "󰣇  {}",
        "interval": 15,
        "tooltip": false,
        "hide-empty-text": true,
    },

    "custom/music": {
        // "exec": "playerctl metadata --ignore-player=firefox --format '{{ title }} - {{ artist }}' --follow",
        "exec": "stdbuf --output=L music-meta",
        "format": "   {}",
        // "interval": once,
        "tooltip": false,
        "hide-empty-text": true,
        "max-length": 40,
        "escape": true,
    },

    "custom/poweroff": {
        "format": "󰐥",
        "tooltip": false,
        "on-click": "systemctl poweroff"
    },

    "custom/reboot": {
        "format": "󰑓",
        "tooltip": false,
        "on-click": "systemctl reboot",
    },

    "custom/lock": {
        "format": "󰌾",
        "tooltip": false,
        "on-click": "hyprlock",
    },

    "group/power": {
        "orientation": "inherit",
        "modules": ["custom/poweroff", "custom/reboot", "custom/lock"],
        "drawer": {
            "transition-left-to-right": true,
        },
    },
}
